#!/usr/bin/env bash
#=
JULIA_PROJECT=@. \
exec julia \
    --startup-file=no \
    --color=yes \
    --compile=yes \
    --optimize=2 \
    -- "${BASH_SOURCE[0]}" "$@"
=#

# generated by Comonicon for the CLI Application nahoquery
using NAHOQuery
exit(NAHOQuery.command_main())
